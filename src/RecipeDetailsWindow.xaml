<Window x:Class="RezeptAnwendung.RecipeDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rezeptdetails" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <!-- Fügen Sie diese Zeile für die Anweisungen hinzu -->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <Label x:Name="TitleLabel" FontWeight="Bold" FontSize="16"/>
        </StackPanel>

        <TextBox x:Name="IngredientsTextBox" Grid.Row="1" Margin="10" TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True"/>

        <TextBlock Grid.Row="2" Margin="10">
            <Hyperlink x:Name="RecipeUrlHyperlink" RequestNavigate="RecipeUrlHyperlink_RequestNavigate"/>
        </TextBlock>

        <TextBox x:Name="PreparationTextBox" Grid.Row="3" Margin="10" TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True"/>

        <Image x:Name="RecipeImage" Grid.Row="4" Margin="10" Width="200" Height="200"/>

        <TextBox x:Name="InstructionsTextBox" Grid.Row="5" Margin="10" TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True"/>
       
    </Grid>
</Window>
